 <page id="gallery" bg="blue">
    <container fab-for="npc in npcs">
      <agent
        id="{{npc.id}}"
        image="{{npc.image}}"
        x="{{npc.x}}"
        y="{{npc.y}}"
        fab-if="npc.visible"/>
    </container>
  </page>